# 2.0.0 发布前人工测试清单

测试日期：__________　测试人：__________　提交版本：2.0.0

每个浏览器都应使用新建的测试配置或测试账号。不要使用包含私人历史、真实同步密钥的截图作为商店素材。

## Safari

- [ ] 在 Xcode 中为宿主 App 和 Extension 选择同一个开发团队，构建无错误。
- [ ] Safari > Settings > Extensions 中可以启用扩展，并只允许访问哔哩哔哩网站。
- [ ] 弹窗中文正常显示，没有乱码。
- [ ] 登录哔哩哔哩后可以保存最近历史。
- [ ] 选择“重新保存全部历史”后，完整保存流程可以结束。
- [ ] 记录列表的搜索、日期、类型筛选和观看进度正常。
- [ ] 分析页只统计普通视频，不计入番剧、直播和专栏。
- [ ] 所有图表在鼠标悬浮或点击数据点时显示具体数值。
- [ ] JSON 备份可以导出并恢复，CSV 可以用 Numbers 打开。
- [ ] 修改时区后，记录列表与逐日图表按所选时区划分日期。
- [ ] 云端同步可完成连接测试、首次上传、第二台浏览器合并。
- [ ] “删除云端记录”能删除 `history.json`，“停用云端同步”能清除本机地址和密钥。

## Chrome

- [ ] 在 `chrome://extensions/` 加载 `.output/chrome-mv3`，没有 manifest 错误。
- [ ] 完成与 Safari 相同的保存、筛选、分析、备份、时区和云同步测试。
- [ ] 扩展的 Service Worker 控制台没有未处理异常。

## Edge

- [ ] 在 `edge://extensions/` 加载 `.output/edge-mv3`，没有 manifest 错误。
- [ ] 完成与 Safari 相同的保存、筛选、分析、备份、时区和云同步测试。
- [ ] 扩展详情页显示的权限仅为功能所需权限。

## Firefox

- [ ] 在 `about:debugging#/runtime/this-firefox` 临时加载 `.output/firefox-mv2/manifest.json`。
- [ ] 完成与 Safari 相同的保存、筛选、分析、备份和时区测试。
- [ ] 第一次使用云同步时出现 Firefox 数据传输授权，拒绝后不发送数据，同意后可以同步。
- [ ] `web-ext lint` 为 0 errors；两条来自 React DOM 的已知 `innerHTML` 警告与 `FIREFOX_REVIEW.md` 一致。

## 商店截图

- [ ] 使用合成数据或专门测试账号，不出现私人账号、书签、邮箱、头像或真实密钥。
- [ ] 准备 `01-history-1280x800.png`、`02-analytics-1280x800.png`、`03-settings-1280x800.png`。
- [ ] 三张图片均为 1280×800 PNG，无浏览器外框、黑边、拉伸或模糊。
- [ ] 设置页截图中的云端地址和同步密钥为空。
- [ ] 截图中的名称、版本和界面均属于 2.0.0。

## 提交前最后确认

- [ ] `pnpm release:package` 全部通过。
- [ ] `.output/SHA256SUMS.txt` 已生成并与五个 ZIP 同时归档。
- [ ] GitHub Pages 的隐私政策和支持页可在无痕窗口打开。
- [ ] 商店文案明确写明“非官方工具”、本地默认保存和云同步默认关闭。
- [ ] 没有把 Firefox sources ZIP 当作扩展安装包上传。
